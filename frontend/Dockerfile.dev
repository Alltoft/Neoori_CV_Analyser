# Dev image: dependencies baked, source bind-mounted by the dev compose so
# `next dev` hot-reloads on save. Rebuild only when package-lock.json changes.
FROM node:22-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm ci
COPY . .
EXPOSE 3001
CMD ["npm", "run", "dev"]
