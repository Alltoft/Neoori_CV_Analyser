# Environment — never commit secrets
.env
.env.*
.env.local
!.env.example

# Python
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
*.egg-info/

# Node / Next.js
node_modules/
.next/
/build
/out
*.tsbuildinfo
next-env.d.ts

# OS
.DS_Store
*.pem

# Logs
npm-debug.log*
yarn-debug.log*
.vercel

# Superpowers SDD scratch (ledger, briefs, review packages)
.superpowers/

# Source instruments — the counselor manual carries the confidential
# option→trait mapping that bank.public() exists to keep server-side.
# Never commit these; the documented deploy ritual is `git add .`.
neoori_cahier_papier.pdf
neoori_scoring_restitution-1.pdf
