[build]
builder = "NIXPACKS"

[deploy]
startCommand = "FLASK_APP=run.py flask db upgrade && gunicorn run:app --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
